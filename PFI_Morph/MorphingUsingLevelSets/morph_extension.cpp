#include "morph_extension.h"
#include <iostream>

// EXPORTED FUNCTIONS ===================================================

double morph_cpp(
        std::string source_obj,
        std::string target_obj, 
        bool cache_objs) {

    // Preprocess both meshes

    // Morph source -> target
    // Morph target -> source

    //Return the energy
    return 0.5;
}

// NON-EXPORTED HELPER FUNCTIONS ========================================

void preprocess_model(std::string model, bool cache) {
    std::cout << "TODO: Preprocess mesh" << std::endl;
}
